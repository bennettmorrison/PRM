<h1>Contacts</h1>
<p>Here's the contacts that we received.</p>
<p>The user id is <%= @app_user_id %>
<p>These contacts have been imported.  The next step is to create a purpose.</p>
<p></p>

<table class="table">
		<tr>
		<th>Name</th>
		<th>First</th>
		<th>Last</th>
		<th>Email</th>
		</tr>
			<% @contacts.each do |c|%>
				<tr>
				<td><%=c[:name]%></td>
				<td><%=c[:first_name]%></td>
				<td><%=c[:last_name]%></td>
				<td><%=c[:email]%></td>
				</tr>	
			<%end%>
</table>